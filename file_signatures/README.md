## Walk and generate file hashes

### Usage
```
Usage of ./run:
  -dest string
    	root direcory for calculate file hashes (default "/tmp")
  -sign string
    	Hashing algorithm (default "md5")
```

### Supported hashes

- MD5SUM
- CRC
- SHA256
